// @ts-nocheck
import { Swal} from "../utils/swal";
import { Translation, translate, category} from "../index";

new Translation(category.english, "Detect Language to English").setClick(async () => {
        translate(auto, en);
});
